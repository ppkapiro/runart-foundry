{
  "name": "briefing",
  "version": "0.1.0",
  "private": true,
  "scripts": {
  "build": "python -m pip install --upgrade pip && pip install -r requirements.txt && mkdocs build --strict -d site"
  }
}
